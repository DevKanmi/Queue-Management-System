# Backend URL (no trailing slash). API and Socket.IO use this.
# Example: http://localhost:5000 → requests go to http://localhost:5000/api/v1
# If unset, client uses relative /api/v1 (Vite proxy to port 5000).
VITE_API_URL=http://localhost:5000
